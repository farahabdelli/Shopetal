{% extends 'base2.html.twig' %}


{% block user %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
    {% if not app.user %}
        {% block fos_user_content %}
        {% endblock fos_user_content %}
    {% else %}

    {% endif %}
            </div>
        </div>
    </div>
{% endblock %}